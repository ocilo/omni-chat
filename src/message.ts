import {Message as MessageInterface} from "./interfaces/message";

/**
 * Represents a high-level message
 */
export class Message implements MessageInterface {
  // How do we differenciate the same message sent in shared discussion to multiple drivers -> it has one id per driver
}
